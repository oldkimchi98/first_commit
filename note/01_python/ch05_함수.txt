5장 함수

1절. 함수의 정의 및 사용 (def 함수명(매개변수..): ~ )
	- 오버로딩(같은 이름의 함수를 매개변수를 달리 사용) 불가
	- docstring(함수 맨 윗줄 설명을 덧붙여서 사용하는데 용이 / 해당 설명을 잘 적을수록 매우 편리함)
	- 전역변수 vs 지역변수(global을 이용하여 함수내에서 전역변수 사용)

2절. 함수의 결과를 반환하는 return
	- return a, b - (a, b) 튜플을 retrun

3절. *중요 함수의 매개변수
	- 매개변수에 기본값을 가질 때 : 함수 호출시 선택적으로 명시
		*기본값이 스칼라변수 : 기본값은 함수정의 시점
		*기본값이 데이터구조, 객체 : 기본값은 함수호출 시점