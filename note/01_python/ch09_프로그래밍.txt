9장 input / output 프로그래밍

with           → 파일을 안전하게 열고 자동으로 닫아준다.
encoding    → 문자 인코딩
r                → 읽기
w		 → 쓰기
a		 → 추가
t		 → 텍스트
b		 → 바이너리
rt		 → Read Text
rb		 → Read Binary
wt		 → Write Text
wb		 → Write Binary
at		 → Append Text
ab		 → Append Binary

1절. txt파일 io
- with open (파일상대경로 | 파일절대경로, 'r | w | a | rt | wt | at', encoding) as f:
	list변수 = f.readlines() / 문자변수 = f.read()
	f.write(저장할 문자열) / f.writelines(문자list)

2절. 피클(pkl, pickle, data) 파일 io
- with open(파일경로, 'rb|wb'
	객체변수 = pickle.load(f)
	pickle.dump(변수, f)

3절. CSV파일 io
- with open(파일경로, 'r | w | a | rt | wt | at', encoding) as f:
	reader = csv.reader(f); result = list(reader)
	write = csv.write(f); writer.writerows(2차원list)

	dic_reader = csv.DictReader(f); 딕셔너리list = list(dic_reader)
	dic_writer = csv.DictWriter(f); dic_wrier.writerows(딕셔너리list)

4절. JSON파일 io
- with open(파일경로, 'r | w | rt | wt | a | at', encoding) as f:
	딕셔너리list - json.load(f)
	json.dump(딕셔너리list, f, ensure_ascii=False, indent)

	객체list = json.load(f, object_hook=딕셔너리를 객체로 return하는 함수명)
	json.dump(객체list, f, default=클래스명. 객체를 딕셔너리로 return함수명)

5절. h5/hdf5 파일 io
- import pandas as pd
	데이터프레임 변수 = pd.read_hdf(파일경로, key)
	데이터프레임 변수.to_hdf(파일경로, key) = 2차원

